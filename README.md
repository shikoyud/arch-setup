# Script for setting up newly install Arch
- Personal use

## Prerequisite
- Arch Linux (fresh install)
- git

## Install
- Clone the reposistory and run the install script
    ```bash
    git clone https://github.com/shikoyud/arch-setup.git
    cd arch-setup
    chmod +x install.sh
    ./install.sh
    ```
